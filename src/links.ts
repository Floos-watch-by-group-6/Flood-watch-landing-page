/**
 * Destination for every app call-to-action — the "Get the app" buttons in the
 * header, floating nav and mobile menu, and the "Give Flood-Watch a try"
 * buttons in the dark section's CTA and the closing band.
 *
 * Kept in one place so the five buttons can never drift apart.
 */
export const APP_URL = 'https://flood-watch-app-j42j.vercel.app'
